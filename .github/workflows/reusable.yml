name: secret scan

on:
  workflow_call:

jobs:
  security:
    runs-on: ubuntu-latest

    steps:
      - name: Check Out Code
        uses: actions/checkout@v4

      - name: Run Linter
        run: npm run lint

      - name: Run Secret Scan
        run: secret-tool scan .
